<p id="notice"><%= notice %></p>

<h3 style="margin: 30px 0px">Listing Articles</h3>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= editable article, :title %></td>
        <td><%= editable article, :content, type: "textarea" %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path %>

<p style="margin: 30px">
  Github: <a herf="https://github.com/diogowernik/x_editable_rails_demo">https://github.com/diogowernik/x_editable_rails_demo</a>
</p>


